import { Doc, Field, Codable, Model, Timestamp, CollectionReference, Collection, firestore } from '@1amageek/ballcap-admin'

export class $$DOCNAME extends Doc {
  static collectionReference(): CollectionReference {
    return firestore.collection('$$DOMAIN').doc('$$PARENTDOCPATH').collection('$$COLLECTIONNAME')
  }
  $$FIELD
  $$SUBCOLLECTION
}